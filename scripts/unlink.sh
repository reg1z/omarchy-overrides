#!/bin/bash
# Remove / sever links to everything imported
# into the the system directories.

3

./link-configs.sh sever # Remove all sync'd configurations
./import-backgrounds.sh sever # Remove Backgrounds
