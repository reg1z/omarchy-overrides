
# ----- appended ----- omarchy-overrides -----

# zsh settings
bindkey -v # vi mode

# system envs
export SSH_AUTH_SOCK=$HOME/.bitwarden-ssh-agent.sock
export PATH="$PATH:$HOME/.local/bin"

# convenience envs
export DOTS=$HOME/repos/omarchy-overrides
export BINS=$HOME/.local/bin

# sourcing
source $DOTS/shell/aliases
source $DOTS/shell/fns

