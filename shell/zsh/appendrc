
# ----- appended ----- omarchy-overrides -----

# zsh settings
bindkey -v # vi mode

# system envs
export SSH_AUTH_SOCK=$HOME/.bitwarden-ssh-agent.sock
export PATH="$HOME/.local/bin:$HOME/.cache/.bun/bin:$PATH:$HOME/.cargo/bin:"
export $OLLAMA_HOST="127.0.0.1:11434"

# convenience envs
export DOTS=$HOME/.local/share/omarchy-overrides
export BINS=$HOME/.local/bin

# sourcing
source $DOTS/shell/aliases
source $DOTS/shell/fns

